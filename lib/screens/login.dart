import 'package:flutter/material.dart';
import 'package:tictacapp/utils/login_body.dart';

class LoginScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(false),
    );
  }
}
